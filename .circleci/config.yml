version: 2
jobs:
  test:
    docker:
      - image: marratj/pipeline-tools:0.4
    steps:
      - checkout
      - run: echo "test"
      - run: terraform init $BACKEND_CONFIG
      - run: terraform plan
workflows:
  version: 2
  test:
    jobs:
      - test